import { app } from 'electron'

// FEAT-01: implement BrowserWindow creation via avatar-window.ts
// FEAT-07: integrate state-store, sidecar, tray, and startup restore flow

app.whenReady().then(() => {
  // implementation coming in FEAT-01 and FEAT-07
})
